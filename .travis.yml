language: node_js
branches:
  only:
    - master
notifications:
  email: false
node_js:
  - '12'
script:
  - yarn test
  - yarn build
# after_success:
  # - npm run travis-deploy-once "npm run semantic-release"
